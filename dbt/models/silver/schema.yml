models:
  - name: silver_games
    description: "Cleaned game data from bronze layer"
    columns:
      - name: game_id
        description: "Game ID"
        tests:
          - unique
          - not_null
      - name: date
        tests:
          - not_null

models:
  - name: dim_teams
    columns:
      - name: team_sk
        tests:
          - unique
          - not_null